{
  "ConnectionStrings": {
    "PostgresTemplate": "Host=localhost;Port=5432;Database=pokemon_portal_db;User Id={userName};Password={password};",
    /*We don't use cache in local envs*/
    "RedisTemplate": "***",
    "SqlServerTemplate": "Server=localhost;Database=pokemon-portal-prd-db;User Id={userName};Password={password};Trusted_Connection=False;Encrypt=False;"

  },

  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000",
      "https://localhost:7204",
      "http://localhost:5214",
      "http://localhost:5651"
    ]
  }
}